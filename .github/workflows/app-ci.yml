on:
  pull_request:
    branches:
      - main
  push:
    branches-ignore:
      - main
  workflow_dispatch:

name: App CI

jobs:
  deploy:
    name: App CI
    runs-on:
      - ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v2

      - name: Node Setup
        uses: actions/setup-node@v1
        with:
          node-version: 12.x

      - name: Resolve Project Dependencies Using Npm
        shell: bash
        run: |
          yarn install